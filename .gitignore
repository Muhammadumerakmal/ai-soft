node_modules/
dist/
.next/
.env
.env.local
.env.development
.env.production
*.log
.DS_Store
*.tsbuildinfo
coverage/
.turbo/

# Claude Code local tooling state (agent worktrees, session data) — never committed
.claude/*
# …except versioned sub-agent definitions, which are shared project config
!.claude/agents/
!.claude/agents/**

# AI-pipeline runtime output — generated at request time, not source
/generated/
docs/*/

