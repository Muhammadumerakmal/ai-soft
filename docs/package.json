{
  "name": "@ai-soft-comp/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Docs placeholder — add a docs framework later'",
    "build": "echo 'Docs placeholder'",
    "lint": "echo 'No linter configured for docs'",
    "typecheck": "echo 'No TS checks for docs'",
    "clean": "echo 'Nothing to clean'"
  }
}
